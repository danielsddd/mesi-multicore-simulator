check C compiler:
gcc --version

compile:
gcc -o sim.exe sim.c -std=c99 -Wall -Werror

run:
./sim.exe

clean directory:
Remove-Item  memout.txt, regout*.txt, core*trace.txt, bustrace.txt, dsram*.txt, tsram*.txt, stats*.txt

git add .

git commit -m "Update docs folder"

git push origin main

# Pull the remote changes first Then push your changes
git pull origin main 

git push origin main